{
  "name": "piter-united",
  "version": "1.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "react-static start",
    "lint": "eslint .",
    "stage": "react-static build --staging",
    "build": "react-static build",
    "publish": "git fetch origin gh-pages && git reset --hard && rm -Rf .git/tmp && mkdir .git/tmp && cp -r dist/ .git/tmp && cp CNAME .git/tmp && git checkout gh-pages && rm -Rf * && cp -r .git/tmp/ . && rm -Rf .git/tmp && git add . && git commit -am 'build' && git push origin gh-pages",
    "serve": "serve dist -p 3000"
  },
  "dependencies": {
    "apollo-cache-inmemory": "^1.0.0",
    "apollo-client": "^2.0.1",
    "apollo-link-http": "^1.1.0",
    "axios": "^0.16.2",
    "bootstrap": "^4.0.0",
    "date-fns": "^1.29.0",
    "graphql": "^0.11.7",
    "graphql-tag": "^2.5.0",
    "json-loader": "^0.5.7",
    "react": "^16.0.0",
    "react-apollo": "^2.0.0",
    "react-dom": "^16.0.0",
    "react-router": "^4.2.0",
    "react-static": "^5.1.14"
  },
  "devDependencies": {
    "eslint-config-react-tools": "1.x.x",
    "serve": "^6.1.0"
  }
}
